extends layout

block content
    .w3-card-4
        header.w3-container.w3-teal
            h3.w3-center= Repositório : #{rep.id}
        .w3-container
                header.w3-container.w3-teal.w3-margin-top
                    h4 Classes
                table(class="w3-table-all")
                    tr
                        th Nome da Classe
                    each c in classes
                        tr
                            td
                                a(href=prefixo + "/repositorio/" + rep.id + "/classe/" + c.classe)= c.classe  
        footer(class="w3-container w3-teal")
            adress.w3-right Diogo Pereira
            a(href="javascript:history.back()") Voltar
            adress /
            a(href="/") Home